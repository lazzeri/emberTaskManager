<div class="flex gap-3 items-start justify-center">
  {{#each @sections as |section|}}
    <MainPage::Section @section={{section}} />
  {{/each}}
  <MainPage::SectionAdder @sections={{@sections}}/>
</div>
